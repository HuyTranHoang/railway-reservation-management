export interface DistanceFare {
  id: number;
  trainCompanyId: number;
  trainCompanyName: string;
  trainCompanyLogo: string;
  distance: number;
  price: number;
  status: string;
  createdAt: string;
}

